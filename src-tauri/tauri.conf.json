{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "tooth-app",
  "version": "0.1.0",
  "identifier": "com.ktc-sc.tooth-app",
  "build": {
    "beforeDevCommand": "mise run build:core-tools && mise run build:3d-matching && mise run build:turntable && bun run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "mise run build:core-tools && mise run build:3d-matching && mise run build:turntable && bun run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "tooth-app",
        "width": 800,
        "height": 600
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": ["deb", "appimage", "rpm"],
    "externalBin": ["binaries/tooth-backend", "binaries/3mserve", "binaries/turntable"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
